# Core (works on macOS / CPU-only)
numpy>=1.24
scikit-image>=0.22
scipy>=1.12
matplotlib>=3.8
streamlit>=1.36
rich>=13.0

# Testing
pytest>=8.0

# Optional GPU (Linux/NVIDIA only)
# Install with: pip install -r requirements.txt --extra-index-url https://pypi.org/simple
# or           pip install .[gpu]
#cupy-cuda12x>=12.0

